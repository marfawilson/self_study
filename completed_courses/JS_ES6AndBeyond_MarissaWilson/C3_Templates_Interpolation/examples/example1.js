const firstname = 'Usagi';
const lastname = 'Tsukino';

// intepolation allows us to build a single string
// and replace certain elements (wrapped with ${}) with other strings
const fullname = `Hello, ${firstname} ${lastname}`;

console.log(fullname);