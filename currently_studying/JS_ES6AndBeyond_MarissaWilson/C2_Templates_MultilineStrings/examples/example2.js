const firstname = 'Marissa';
const lastname = 'Wilson';

// use backticks in order to use template literals & create multiline strings easier
const fullname = `Hello,
` + firstname + `
` + lastname;

console.log(fullname);